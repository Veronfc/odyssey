{
  "name": "odyssey",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.11"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "private": true,
  "scripts": {
    "client:dev": "bun --filter client dev",
    "server:dev": "bun --filter server dev",
    "dev": "bun --filter client dev && bun --filter server dev",
    "format": "bunx prettier . --write"
  },
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ]
}
